import * as AeonMath from '../AeonMath';

test('square', () => {
	expect(AeonMath.square).toBeDefined();
});

test('sqrt', () => {
	expect(AeonMath.sqrt).toBeDefined();
});

test('getRandomInt', () => {
	expect(AeonMath.getRandomInt).toBeDefined();
});

test('Point', () => {
	expect(AeonMath.Point).toBeDefined();
});

test('NormalRandom', () => {
	expect(AeonMath.normalRandom).toBeDefined();
});